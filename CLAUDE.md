# 눌러도돼? (click-safe)

청소년이 받은 의심스러운 링크를 검사하고, 왜 위험한지 설명하고,
이미 클릭한 경우 대처법을 안내하는 웹 서비스.

## 스택
Next.js 15 (App Router) / TypeScript / Tailwind CSS / Vercel

## 절대 규칙
1. **판정은 코드가, 설명은 LLM이.** 위험도 점수는 `lib/score.ts`의 순수 함수가
   계산한다. LLM에게 "이 URL이 위험한가?"를 묻지 않는다.
2. **원본 페이지 콘텐츠를 LLM에 넣지 않는다.** 프롬프트 인젝션 방지.
   urlscan.io/VirusTotal이 반환한 구조화된 필드값만 전달한다.
3. **서버가 대상 URL에 직접 접속하지 않는다.** 모든 접속은 urlscan.io가 대행한다.
   `fetch(userProvidedUrl)` 같은 코드를 절대 작성하지 않는다.
4. **API 키는 서버 사이드에서만 사용한다.** 클라이언트 컴포넌트에 노출 금지.
5. **사용자가 입력한 URL을 로그·DB에 저장하지 않는다.**

## 사용자 문구 톤
- 독자는 13~18세. 기술 용어 금지.
- 겁주거나 책망하지 않는다. 이미 피해를 본 사람이 읽는다.

## 명령어
- `npm run dev` — 개발 서버
- `npm run build` — 빌드 검증 (커밋 전 필수)

## 참고
전체 요구사항은 `PRD.md` 참조.
